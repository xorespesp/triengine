#include "file_bvh_loader.hh"
#include "../misc/debug_utils.hh"

namespace triengine::io
{
    bool load_obj_file(
        const std::filesystem::path& file_path,
        std::vector<geometry::skeleton_object>& frames)
    {
        return false;
    }

} // namespace